# 更新记录

## [Unreleased]

### 变更

- 统一版本叙述：V1.0 是当前对外工作流版本，V0.4 仅作为历史执行闭环编号保留；数据合同版本继续独立记录为 motion_plan 0.3 与其他合同 0.1。
- 优化 Skill 入口与 V1.0 总控：按阶段加载参考，保留局部请求范围；统一已有分镜的授权复用规则，避免重复确认和无必要重绘。
- 预览按实际内容选择检查项，无对白作品不强加对白；明确 preview 脚本渲染全长、状态检查不代表验收、局部返工需更新受影响依赖，并保留恢复所需文件。
- 修正契约版本与自动修复范围的过时说明，保留现有 Schema 和渲染实现。
- 清理旧入口文档中的 0.2/0.3、视差和“必须先确认角色”冲突；明确 0.2 仅为兼容合同，新增文档一致性回归检查。
- 正式入口改为由 Skill 根据用户 brief 生成角色、master 和漫画分镜。
- 删除直接把完整漫画或单张分镜作为最终视频镜头的工作模式。
- 上传图片只作为可选身份或画风参考，不直接进入最终 shot。
- 质量闸门增强时间轴事件阶段、眨眼帧和 CUT 边界检查，为 V0.5 的局部表演扩展提供更早的结构性反馈。
- 新增 V1.0 总控系统参考：项目状态机、入口分流、最小提问、反馈路由和最小修改原则。
- 新增 `scripts/preview.py` 一体化预览入口：串联素材校验、质量闸门、提示词编译、Remotion prepare 与 MP4 渲染，并将结果复制回独立项目目录。
- 增加预览编排回归测试；当前本地测试共 57 项通过。
- 状态检查接入四份生产合同的完整 Schema 错误路径，并新增 `revision_log.json` 与 `revision add/update` 返工台账命令。
- 新增 `doctor.py` 本地 Python/Node/npm/Remotion 环境诊断，以及 `preview.py --incremental` 的逐镜素材指纹缓存；缓存只生成逐镜预览，不替代全片交付渲染。
- 新增 `deliver.py` 交付闸门：统一检查 Schema/资产、质量报告、视觉复核新鲜度、返工台账和 ffprobe MP4 元数据，并输出 `delivery_report.json`。
- 新增可选 `content_strategy` 内容层和短视频内容策划参考：先确定受众、平台、钩子、核心承诺与结尾收获，再进入角色、分镜和动画制作。
- 新增生图规范与提示词追溯字段：区分角色参考、当镜 master 和图层提取，统一身份范围、负面约束、prompt 版本、镜头 ID、参考图用途和模型/seed 记录。
- 新增 `simple-comic` 风格预设：提炼参考图的粗线、平涂、夸张表情和简化背景，禁止生成对白气泡，统一预留后期字幕安全区。
- 质量闸门新增可见对白角色的嘴型驱动检查，防止字幕和配音存在但人物没有可见说话表演。
- 明确知识卡、总结卡和 CTA 只在用户提出并选择时启用，默认不追加收束卡。
- 新增 `comic-economy` 生图档位和头部/手部/道具固定相机回归夹具，验证动态漫画的重点是可读表演而非过度精致的背景；本地测试增至 61 项。

## [0.4.0] - 2026-09-21

### 新增

- 将制作向导、角色一致性与自然表演、音频驱动时间轴、漫画视听反馈和预览前质量闸门整合为一条可追踪流程。
- 增加 Critical/Major 优先的局部自动修正边界，避免因字幕、动作或音效问题重做整部视频。
- 提供一体化安装脚本，一次准备 Agent Skill、Python 校验环境和 Remotion 渲染器。
- 明确 Skill 发布版本与 JSON 合同版本分离：V0.4 继续读取 `motion_plan` 0.3 和其他 0.1 合同。

### 兼容与边界

- V0.3 的项目可以继续使用，不需要迁移 JSON 版本；建议重新运行质量闸门后再生成预览。
- 本版仍不包含精确音素口型、骨骼绑定、完整逐帧动画、自动抠图或无人审核的全自动成片。
- V0.4 已通过真实连续镜头样片的角色一致性、动作自然度、音画同步、字幕可读性和 CUT 节奏验收，可标记为稳定版本。

## [0.3.0] - 2026-09-20

### 新增

- 连续分镜式动态漫画的项目启动分流：完整漫画、单张分镜、角色设定或从零生成。
- 17 项逐镜审阅字段，包含来源漫画格、说话者、音效和切镜衔接。
- 事件触发的角色动作校验，阻止周期性漂浮、机械呼吸和镜头推拉伪动态。
- 场景空间连续性、角色身份连续性和背景构图变化规则。
- MIT 开源许可证、贡献流程和本地素材隐私边界。

### 验证

- 本地测试：42 项通过。
- GitHub Actions：行为检查、合同校验和 Remotion MP4 渲染通过。
